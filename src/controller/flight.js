const helper = require("../helper/response");

module.exports = {
  postFlight: async (req, res) => {
    try {
      const {
        mascapai,
        facilities,
        departureTime,
        arrivedTime,
        price,
        capacity,
        class,
        fromCity,
        fromCountry,
        toCity,
        toCountry,
        tripType,
        terminal,
        transitType,
      };
    } catch (error) {}
  },
};
